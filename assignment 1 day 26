#include<stdio.h>
int main()
{
	int t;
	scanf("%d",&t);
	while(t--)
	{
		int n,a[100],i,s=0;
		scanf("%d",&n);
		for(i=0;i<n;i++)
		{
			scanf("%d",&a[i]);
		}
		for(i=0;i<n;i++)
		{
			s=s+a[i];
		}
		for(i=0;i<n;i++)
		printf(" %d",s-a[i]);
	}
}
